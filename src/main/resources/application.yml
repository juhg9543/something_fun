spring.application.name=something_fun
server.port=8080

# Logging
logging.level.root=INFO
logging.level.com.example.something_fun=DEBUG
spring:
  security:
    oauth2:
      client:
        registration:
          github:
            clientId: ${GITHUB_CLIENT_ID}
            clientSecret: ${GITHUB_CLIENT_SECRET}
# ...